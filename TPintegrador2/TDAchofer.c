#include <stdio.h>
#include <stdlib.h>
#include "TDAchofer.h"

choferPtr crearChofer(char* nombre, char* apellido, domicilioPtr, int cuil){
    choferPtr chof=(choferPtr)obtenerMemoria(sizeof(chofer));
    chof->nombre,apellido,domicilioPtr,cuil;







}
